import { createTheme } from "@mui/material";

export const ThemeOptions = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#3f51b5",
    },
    secondary: {
      main: "#f50057",
    },
  },
  typography: {
    fontFamily: "Open Sans",
  },
});
